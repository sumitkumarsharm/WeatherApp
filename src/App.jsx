import React, { useState } from "react";
const App = () => {
  return (
    <div
      className={`w-full min-h-screen flex items-center justify-center bg-gradient-to-r   p-3 transition-colors duration-500`}
    ></div>
  );
};

export default App;
